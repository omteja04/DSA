/**
 * Author: omteja04
 * Created on: 29-09-2024 19:05:49
 * Description: ModularArithmetic
 **/

/*
 *
 * (a + b) % M = ((a % M) + (b % M)) % M
 * (a * b) % M = ((a % M) * (b % M)) % M
 * (a - b) % M = ((a % M) - (b % M) + M) % M
 * (a / b) % M = ((a % M) * (b ^ -1) % M) % M




 */